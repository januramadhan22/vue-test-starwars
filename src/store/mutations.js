export function setSearchCharacters(state, characters) {
  state.searchedCharacters = characters;
}
export function setGetCharacters(state, charactersList) {
  state.getCharacters = charactersList;
}
